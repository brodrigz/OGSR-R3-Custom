#include "common.h"

#ifdef USE_RSM
#include "ogsr_rsm_capture.h"
RSMCapture main(v2p_shadow_direct I)
{
    return CaptureRSM(s_base.Sample(smp_linear, I.tc0).rgb, I.rsm_world, I.hpos.z);
}
#else

//////////////////////////////////////////////////////////////////////////////////////////
// Pixel
// float4 main ( v_shadow_direct I ) : SV_Target
float4 main() : SV_Target
{
#ifdef USE_HWSMAP
    return 0;
#else
    return I.depth;
#endif
}
#endif
